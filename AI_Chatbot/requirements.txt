# Core (required)
scikit-learn>=1.3
flask>=2.3

# NLP preprocessing - pick ONE, spaCy is preferred (bonus feature)
spacy>=3.7
# nltk>=3.8          # fallback backend if you don't want to install spaCy

# Bonus: pretrained transformer intent classification
transformers>=4.35
torch>=2.1

# Bonus: voice input/output
SpeechRecognition>=3.10
pyttsx3>=2.90
pyaudio>=0.2.13   # may require system audio libs (e.g. portaudio) to build
