pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Optional but recommended for better preprocessing (bonus feature).
# The system automatically falls back to NLTK, and then to plain regex
# cleaning, if these aren't installed — so they are NOT strictly required.
spacy>=3.7.0
nltk>=3.8.0
